What happens when you initialize a repository? Why do you need to do it?
	new file called .git creatted, we need this to keep the historical saved.

How is the staging area different from the working directory and the repository?
What value do you think it offers?

	the staging area is a status of files before comiting and after changing. it makes to add all the changes needed to make a commit

How can you use the staging area to make sure you have one commit per logical
change?

	by adding one file to it itch time then commit it after that adding the second file and so on.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

	when you want to try something deferent 
	or when you want to create or add new feature


How do the diagrams help you visualize the branch structure?

	the diagrams give you the idea of how the branch structure looks like

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

	the result of mergin two branches together is new branche that have all new features that have the two branches.
	and the reason of present it in diagram is to know which commits are reachible

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?

	merges manually gives the control